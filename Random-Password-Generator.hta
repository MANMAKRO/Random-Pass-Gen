<html>
<head>
    <title>Password Generator</title>
    <HTA:APPLICATION
        APPLICATIONNAME="Password Generator"
        BORDER="thin"
        INNERBORDER="no"
        ICON=""
        SCROLL="no"
        SHOWINTASKBAR="yes"
        SINGLEINSTANCE="yes"
        SYSMENU="yes"
        SELECTION="no"
        MAXIMIZEBUTTON="no"
        MINIMIZEBUTTON="yes"
        RESIZE="no"
        CONTEXTMENU="no"
        NAVIGABLE="no"
        BORDERSTYLE="normal"
        CAPTION="yes"
    />
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        .button-container {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .button-container button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #007bff;
            color: #fff;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .button-container button:hover {
            background-color: #0056b3;
        }

        #passwordOutput {
            width: 100%;
            height: 120px;
            padding: 10px;
            font-size: 16px;
            resize: none;
        }

        .footer-text {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #555;
        }

        .footer-text a {
            color: #007bff;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer-text a:hover {
            color: #0056b3;
        }

        .app-description {
            text-align: center;
            margin-top: 20px;
            font-size: 16px;
            color: #555;
        }
    </style>
    <script language="vbscript">
        Option Explicit

        Dim passwordLength
        Dim password

        Sub GeneratePassword
            passwordLength = InputBox("Enter the length of the password", "Password Length")

            Dim validCharacters
            validCharacters = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*()_-+=<>?/;:~"

            password = ""

            Randomize

            Dim i
            For i = 1 To passwordLength
                Dim randomIndex
                randomIndex = Int((Len(validCharacters) * Rnd) + 1)
                password = password & Mid(validCharacters, randomIndex, 1)
            Next

            passwordOutput.Value = password
        End Sub

        Sub OpenWebsite
            Dim objShell
            Set objShell = CreateObject("Shell.Application")
            objShell.ShellExecute "https://www.256bits.tech"
            Set objShell = Nothing
        End Sub
    </script>
</head>
<body>
    <h1>Password Generator</h1>
    <div class="button-container">
        <button onclick="GeneratePassword">Generate Password</button>
    </div>
    <textarea id="passwordOutput" readonly></textarea>
    <div class="footer-text">
        This program was made by <a href="#" onclick="OpenWebsite()">www.256Bits.tech</a>
    </div>
    <div class="app-description">
        <p>A simple password generator that allows you to generate strong, random passwords of the desired length.</p>
        <p>The randomness is achieved by utilizing a specialised `Randomizie` function to initialize the random number generator with a unique seed value based on the system's internal clock. This ensures that each run of the program produces a different sequence of random numbers.</p>
	<p>NOTE: To copy the password, highlight it and then use CTRL+C.</p>
</body>
</html>
